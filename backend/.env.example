# Copy this file to .env and fill in real values for local development.
# Never commit the real .env file — it is gitignored.
#
# This is a placeholder for Milestone 2 (Configuration Management), which
# will define the actual settings schema. No config system exists yet.

APP_NAME=product-intelligence-backend
ENVIRONMENT=local
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8000
