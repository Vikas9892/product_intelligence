# Backend-specific ignores (repo-wide rules live in the root .gitignore)
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.env
!.env.example

# Runtime-created (app/core/paths.py::ensure_runtime_directories) — never
# source-controlled; recreated automatically at app startup if missing.
storage/
